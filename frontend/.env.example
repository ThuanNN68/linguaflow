# Public values embedded into the browser bundle at build time.
# Do not place API secrets or Supabase service-role credentials here.

# Required for production builds. Empty uses the /api rewrite only in development.
NEXT_PUBLIC_API_URL=

# Optional Google Identity Services Web Client ID.
NEXT_PUBLIC_GOOGLE_OAUTH_CLIENT_ID=
